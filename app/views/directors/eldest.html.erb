<h1>Eldest director</h1>

<% director = Director.where.not({ :dob => nil }).order({ :dob => :asc }).at(0) %>

<p>The eldest director in our dataset right now is <%= director.name %>, born on <%= director.dob.strftime(%B %e,%Y) %>.</p>
