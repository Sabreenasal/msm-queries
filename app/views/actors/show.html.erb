<div>
  <div>
    <h1>
      Actor #<%= @the_actor.id %> details
    </h1>

    <div>
      <div>
        <a href="/actors">
          Go back
        </a>
      </div>

      <div>
        <a href="/delete_actor/<%= @the_actor.id %>">
          Delete actor
        </a>
      </div>
    </div>

    <dl>
      <dt>
        Name
      </dt>
      <dd>
        <%= @the_actor.name %>
      </dd>

      <dt>
        Dob
      </dt>
      <dd>
        <%= @the_actor.dob %>
      </dd>

      <dt>
        Image url
      </dt>
      <dd>
        <%= @the_actor.image %>
      </dd>

      <dt>
        Created at
      </dt>
      <dd>
        <%= time_ago_in_words(@the_actor.created_at) %> ago
      </dd>

      <dt>
        Updated at
      </dt>
      <dd>
        <%= time_ago_in_words(@the_actor.updated_at) %> ago
      </dd>
    </dl>
  </div>
</div>

<hr>


<div>
  <div>
    <h2>
      Edit actor
    </h2>

    <form action="/modify_actor/<%= @the_actor.id %>"  method="post" >
      <div>
        <label for="name_box">
          Name
        </label>

        <input type="text" id="name_box" name="query_name" value="<%= @the_actor.name %>">
      </div>

      <div>
        <label for="dob_box">
          Dob
        </label>

        <input type="date" id="dob_box" name="query_dob" value="<%= @the_actor.dob %>">
      </div>

      <div>
        <label for="image_url_box">
          Image url
        </label>

        <input type="text" id="image_url_box" name="query_image_url" value="<%= @the_actor.image %>">
      </div>

      <button>
        Update actor
      </button>
    </form>
  </div>
</div>

<hr>

<% the_characters = Character.where({ :actor_id => @the_actor.id })%>

<ul>
  <% the_characters.each do |character| %>
  <% movie = Movie.where({ :id => character.movie_id }).at(0) %>
    <li>
      <% character.movie_id %>
      <% character.name %>
    </li>

  <% end %>
</ul>
